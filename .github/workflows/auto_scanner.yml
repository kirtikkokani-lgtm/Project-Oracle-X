run: |
          python -c "
          import yfinance as yf
          import requests
          import os

          token = os.getenv('TOKEN')
          chat_id = os.getenv('CHAT_ID')
          
          # टेस्टिंगसाठी एक मेसेज
          test_url = f'https://api.telegram.org/bot{token}/sendMessage'
          params = {'chat_id': chat_id, 'text': '🚨 Oracle-X Connection Test Success!'}
          
          response = requests.get(test_url, params=params)
          print(f'Telegram Response: {response.json()}') # हे आपल्याला चुका शोधायला मदत करेल
          "
